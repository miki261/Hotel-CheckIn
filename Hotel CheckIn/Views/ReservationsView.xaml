<UserControl x:Class="Hotel_CheckIn.Views.ReservationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
	<Grid>
		<Border Background="{DynamicResource BgDark}" CornerRadius="12" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Padding="30">
			<Border.Effect>
				<DropShadowEffect BlurRadius="30" ShadowDepth="4" Direction="270" Color="Black" Opacity="0.1"/>
			</Border.Effect>
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="*"/>
				</Grid.RowDefinitions>
				<TextBlock Text="Current Reservations" Grid.Row="0" FontSize="26" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,20"/>
				<Border Grid.Row="1" Background="{DynamicResource ElementBg}" CornerRadius="8" BorderThickness="1" BorderBrush="#334155" Margin="0,0,0,20" Height="42">
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="Auto"/>
							<ColumnDefinition Width="*"/>
						</Grid.ColumnDefinitions>
						<TextBlock Text="🔍" Grid.Column="0" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" Margin="15,0,5,0" FontSize="14" FontFamily="Segoe UI Emoji"/>
						<TextBox x:Name="SearchReservationsTextBox" Grid.Column="1" Background="Transparent" Foreground="{DynamicResource TextPrimary}" BorderThickness="0" VerticalContentAlignment="Center" Padding="5,0,10,0" FontSize="14" TextChanged="SearchReservationsTextBox_TextChanged" ToolTip="Search by guest, ID, email, or room" />
					</Grid>
				</Border>
				<Border Grid.Row="2" CornerRadius="8" BorderThickness="1" BorderBrush="#334155" Background="{DynamicResource ElementBg}" ClipToBounds="True">
					<ListView x:Name="ReservationsList" Background="Transparent" Foreground="{DynamicResource TextPrimary}" BorderThickness="0" HorizontalContentAlignment="Stretch">
						<ListView.Resources>
							<Style TargetType="GridViewColumnHeader">
								<Setter Property="Background" Value="{DynamicResource SidebarDark}"/>
								<Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
								<Setter Property="FontWeight" Value="SemiBold"/>
								<Setter Property="Padding" Value="15,12"/>
								<Setter Property="BorderThickness" Value="0,0,0,1"/>
								<Setter Property="BorderBrush" Value="#334155"/>
								<Setter Property="HorizontalContentAlignment" Value="Left"/>
							</Style>
							<Style TargetType="ListViewItem">
								<Setter Property="Focusable" Value="False"/>
								<Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
								<Setter Property="Template">
									<Setter.Value>
										<ControlTemplate TargetType="ListViewItem">
											<Border x:Name="Bd" Background="Transparent" BorderThickness="0,0,0,1" BorderBrush="#334155" Padding="5,10">
												<GridViewRowPresenter Content="{TemplateBinding Content}" Columns="{TemplateBinding GridView.ColumnCollection}"/>
											</Border>
											<ControlTemplate.Triggers>
												<Trigger Property="IsMouseOver" Value="True">
													<Setter TargetName="Bd" Property="Background" Value="{DynamicResource ElementBg}"/>
													<Setter TargetName="Bd" Property="Opacity" Value="0.8"/>
												</Trigger>
											</ControlTemplate.Triggers>
										</ControlTemplate>
									</Setter.Value>
								</Setter>
							</Style>
							<Style x:Key="CheckoutButtonStyle" TargetType="Button">
								<Setter Property="Background" Value="#EF4444"/>
								<Setter Property="Foreground" Value="White"/>
								<Setter Property="BorderThickness" Value="0"/>
								<Setter Property="Padding" Value="10,6"/>
								<Setter Property="FontWeight" Value="SemiBold"/>
								<Setter Property="Cursor" Value="Hand"/>
								<Setter Property="Template">
									<Setter.Value>
										<ControlTemplate TargetType="Button">
											<Border Background="{TemplateBinding Background}" CornerRadius="6">
												<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
											</Border>
											<ControlTemplate.Triggers>
												<Trigger Property="IsMouseOver" Value="True">
													<Setter Property="Background" Value="#DC2626"/>
												</Trigger>
											</ControlTemplate.Triggers>
										</ControlTemplate>
									</Setter.Value>
								</Setter>
							</Style>
							<Style x:Key="EditButtonStyle" TargetType="Button">
								<Setter Property="Background" Value="{DynamicResource AccentBlue}"/>
								<Setter Property="Foreground" Value="White"/>
								<Setter Property="BorderThickness" Value="0"/>
								<Setter Property="Padding" Value="15,6"/>
								<Setter Property="FontWeight" Value="SemiBold"/>
								<Setter Property="Cursor" Value="Hand"/>
								<Setter Property="Template">
									<Setter.Value>
										<ControlTemplate TargetType="Button">
											<Border Background="{TemplateBinding Background}" CornerRadius="6">
												<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
											</Border>
											<ControlTemplate.Triggers>
												<Trigger Property="IsMouseOver" Value="True">
													<Setter Property="Background" Value="#2563EB"/>
												</Trigger>
											</ControlTemplate.Triggers>
										</ControlTemplate>
									</Setter.Value>
								</Setter>
							</Style>
						</ListView.Resources>
						<ListView.View>
							<GridView>
								<GridViewColumn Header="Guest" Width="120" DisplayMemberBinding="{Binding FullName}" />
								<GridViewColumn Header="ID / Passport" Width="120" DisplayMemberBinding="{Binding IdPassport}" />
								<GridViewColumn Header="Email" Width="160" DisplayMemberBinding="{Binding Email}" />
								<GridViewColumn Header="Room" Width="60" DisplayMemberBinding="{Binding RoomNumber}" />
								<GridViewColumn Header="Check-In" Width="120" DisplayMemberBinding="{Binding CheckInDate, StringFormat=dd.MM.yyyy HH:mm}" />
								<GridViewColumn Header="Check-Out" Width="120" DisplayMemberBinding="{Binding CheckOutDate, StringFormat=dd.MM.yyyy HH:mm}" />
								<GridViewColumn Header="Edit" Width="80">
									<GridViewColumn.CellTemplate>
										<DataTemplate>
											<Button Content="Edit" Style="{StaticResource EditButtonStyle}" Click="EditReservation_Click"/>
										</DataTemplate>
									</GridViewColumn.CellTemplate>
								</GridViewColumn>
								<GridViewColumn Header="Action" Width="100">
									<GridViewColumn.CellTemplate>
										<DataTemplate>
											<Button Content="Check Out" Style="{StaticResource CheckoutButtonStyle}" Click="CheckOut_Click"/>
										</DataTemplate>
									</GridViewColumn.CellTemplate>
								</GridViewColumn>
							</GridView>
						</ListView.View>
					</ListView>
				</Border>
			</Grid>
		</Border>
	</Grid>
</UserControl>