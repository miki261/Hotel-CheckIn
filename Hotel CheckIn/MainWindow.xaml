<Window x:Class="Hotel_CheckIn.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:Hotel_CheckIn.Views"
        Title="Hotel Check-In"
        Height="500" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#0F172A">

	<!-- Styles -->
	<Window.Resources>
		<Style x:Key="NavButton" TargetType="Button">
			<Setter Property="Margin" Value="10"/>
			<Setter Property="Height" Value="50"/>
			<Setter Property="Background" Value="#2563EB"/>
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="BorderThickness" Value="0"/>
			<Setter Property="Cursor" Value="Hand"/>
			<Setter Property="FontSize" Value="14"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Button">
						<Border Background="{TemplateBinding Background}" CornerRadius="10">
							<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter Property="Background" Value="#3B82F6"/>
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
	</Window.Resources>

	<Grid>
		<!-- Grid -->
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="220"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>

		<!-- Sidebar -->
		<StackPanel Background="#020617" Grid.Column="0">
			<TextBlock Text="HOTEL"
                       Foreground="White"
                       FontSize="22"
                       Margin="20"
                       FontWeight="Bold"/>

			<Button Content="New Guest" Click="NewGuest_Click" Style="{StaticResource NavButton}"/>
			<Button Content="Current Reservations" Click="Reservations_Click" Style="{StaticResource NavButton}"/>
			<Button Content="History" Click="History_Click" Style="{StaticResource NavButton}"/>
		</StackPanel>

		<!-- Content -->
		<ContentControl x:Name="MainContent" Grid.Column="1"/>
	</Grid>

</Window>
